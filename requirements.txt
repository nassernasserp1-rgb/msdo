# المكتبات الأساسية للسكرابة
playwright>=1.40.0
aiohttp>=3.8.0
asyncio-throttle>=1.0.2

# قاعدة البيانات والتخزين
sqlite3  # مدمجة مع Python
aiosqlite>=0.19.0

# واجهة المستخدم
customtkinter>=5.2.0
Pillow>=10.0.0

# معالجة البيانات
pandas>=2.0.0
numpy>=1.24.0

# الشبكة والطلبات
requests>=2.31.0
urllib3>=2.0.0

# التاريخ والوقت
python-dateutil>=2.8.0

# التوازي والمعالجة المتزامنة
concurrent-futures>=3.1.1  # مدمجة مع Python 3.2+

# اللوجينج والتتبع
colorama>=0.4.6
tqdm>=4.65.0

# JSON والبيانات المنظمة
ujson>=5.8.0  # أسرع من json العادية

# التحسينات الإضافية
lxml>=4.9.0  # لتحليل HTML أسرع
beautifulsoup4>=4.12.0  # احتياطي لتحليل HTML

# المراقبة والأداء
psutil>=5.9.0  # لمراقبة استخدام الموارد
memory-profiler>=0.61.0  # لمراقبة الذاكرة

# الأمان والتشفير
cryptography>=41.0.0

# اختياري: لتحسين الأداء
uvloop>=0.17.0  # حلقة أحداث أسرع لـ Linux/macOS
orjson>=3.9.0  # JSON أسرع

# للتطوير والاختبار (اختياري)
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.0.0
flake8>=6.0.0